#include<stdio.h>
#include <stdlib.h>
#include<string.h>





int main(){
    free(x[i]);
    }
    free(x);
    int *p;
    int *y=(int *)malloc(5*sizeof(int));
    realloc(y,7);
    for(int k=0;k<7;++k){
       *(y+k)=k+15;
    }
    for(int k=0;k<7;++k){
        printf("%d\n",y[k]);
    }
    free(p);
    
}